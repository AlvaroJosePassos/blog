<h1><%= @article.title %></h1>

<p><%= @article.body %></p>

<ul>
    <li><%= button_to "Edit", edit_article_path(@article), method: :get %></li>
    <li><%= button_to "Delete", article_path(@article), method: :delete, data: { turbo_confirm: 'operate.delete_confirm?'} %>
    </li>
    <li><%= button_to "Back", root_path, method: :get %></li>
</ul>

This article has <%= Comment.public_count %> comments

<h2>Comments</h2>
<%= render @article.comments %>

<h2>Add a comment:</h2>
<%= render 'comments/form' %>